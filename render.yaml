services:
  - type: web
    name: attendance-permit
    runtime: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn 'app.app:app' --timeout 120"
    envVars:
      - key: DATABASE_URL
        sync: false
